#ifndef ENTITY_HPP
#define ENTITY_HPP

#include <string>
#include <iostream>
#include <fstream>
#include <stdio.h>
#include <cstring>
#include <cassert>
#include <cmath>
#include <cstdlib>

static const uint8_t DEFSIZE = 100;
static const uint8_t RAM = 128;
static const uint8_t REG = 64;
static const uint8_t CONST = 32;
static const uint8_t MASK = 31;
static const uint8_t PASSNUM = 2;
static const uint32_t DAM = -1;
static const char *REGISTERS = "[rax] [rbx] [rcx] [rdx]";

#endif //ENTITY_HPP